package model;


/*Fancy is the main class from where the system is commencing
*/
public class fancy {
	 
	public static void main(String[] args) {
	//	CityLodge cl= new CityLodge();
		try {
			
			/*
			 * CreateTable ct = new CreateTable(); ct.createNewTable(); DatabaseConnection
			 * dbConnection = new DatabaseConnection(cl); DmlOperations dml = new
			 * DmlOperations(); dml.getRoomEntries(cl);
			 */
			//System.out.println("done");
			
			
			 
			// System.out.println("okay bye");
		} catch (Exception e) {
			System.out.println(e.getMessage());
		}
		
		//cl.runApp();
	
		

	}

}
